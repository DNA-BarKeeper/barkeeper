<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">Caryophyllales matK - Assembled contigs that need verification</h3>
  </div>
  <div class="panel-body">

    <div class="wideTable">
      <table id="contigs" data-source="<%= caryophyllales_need_verification_contigs_url(format: "json") %>" class="table table-striped table-hover table-bordered">
        <thead>
        <tr>
          <th>Name</th>
          <th>Species</th>
          <th>Specimen</th>
          <th>Assembly complete?</th>
          <td>Last updated</td>
          <td></td>
        </tr>
        </thead>

        <tbody>
        </tbody>
      </table>
    </div>

  </div>
</div>

<p>
  <%= link_to 'Export contigs to zip…', create_caryo_contigs_zip_path, :class => 'btn btn-default' %>
  <%= link_to 'Download last contig export', caryo_contigs_zip_path, :class => 'btn btn-default' %>
</p>

<p>
  Assembly verified for <%= Contig.verified.count %> contigs.
</p>