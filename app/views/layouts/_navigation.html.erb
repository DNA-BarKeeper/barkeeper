<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">

  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if ENV['HEROKU_RELEASE_VERSION'] %>
          <% version = ENV['HEROKU_RELEASE_VERSION'] %>
      <% else %>
          <% version = 'v123' %>
      <% end %>
      <% if ENV['HEROKU_RELEASE_CREATED_AT'] %>
          <% date = ENV['HEROKU_RELEASE_CREATED_AT'] %>
          <% date=Time.zone.parse(date) %>
      <% else %>
          <% date = Time.zone.now %>
      <% end %>
      <%= link_to  about_path, class: 'navbar-brand', :title => "Released #{distance_of_time_in_words(Time.zone.now, date)} ago"  do %>
          GBOL5 <span style="color: #616161">Web App <span style="font-size: xx-small"> <%= version %></a></span></span>
      <% end %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <%= render 'layouts/navigation_links' %>
      </ul>
    </div>
  </div>

</nav>

