<div class="panel panel-default">

  <div class="panel-heading">
    <h3 class="panel-title">Plates (DNA)</h3></div>

  <div class="panel-body">
    <div class="wideTable">
      <table id="micronic_plates" data-source="<%= micronic_plates_url(format: 'json')%>" class="table table-striped table-hover table-bordered">
        <thead>
        <tr>
          <th>Name/Code</th>
          <th>Rack</th>
          <th>Rack Position</th>
          <th>Shelf</th>
          <th>Freezer</th>
          <th>Last updated</th>
          <th ></th>
        </tr>
        </thead>

        <tbody>
        </tbody>
      </table>
    </div>
  </div>

</div>

<p>
  <%= link_to new_micronic_plate_path, class: 'btn btn-default', title: 'Create a new plate (DNA) entry' do %>
    <span class="glyphicon glyphicon-unchecked"></span>
  <% end %>
  Create a new plate (DNA) entry
</p>
