<div class="form-group">
  <%= form.label :project_ids, 'Projects this object belongs to:' %>
  <br>
  <%= form.collection_select :project_ids, Project.all.order(:name), :id, :name, {}, { class: 'form-control', multiple: true }  %>
</div>